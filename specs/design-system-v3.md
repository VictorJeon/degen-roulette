# Degen Roulette — Design System v3 Spec

> 레퍼런스 4장 분석 + 현재 코드 상태 기반. v0.dev 프로토타이핑 & Tailwind 마이그레이션 전 방향 문서.

---

## 1. 레퍼런스 분석 요약

### 1-1. Death.Fun (`ref-death-fun.png`)
- **컬러**: 순수 블랙 배경(`#0A0A0A`), 차트레즈 그린 악센트(`#BEFF00`). 네온이지만 **한 색상만** 쓰고 나머지는 그레이스케일.
- **레이아웃**: 좌측 사이드바(네비게이션) + 중앙(게임 그리드) + 우측 사이드바(리더보드). 카드 기반 그리드.
- **타이포**: 산세리프, 작고 밀도 높은 텍스트. 볼드는 수치에만. 위계가 색상(그린=강조, 화이트=기본, 그레이=보조)으로 구분됨.
- **핵심 원칙**: **단색 악센트 + 극단적 미니멀리즘**. 네온을 쓰지만 절제돼있어서 고급스러움.

### 1-2. Coinflip Dark Mode (`ref-coinflip-dark.png`)
- **컬러**: 네이비 다크(`#0F1923`), 오렌지/앰버(`#F5A623`)=Heads, 틸/시안(`#3DC8D8`)=Tails. 에러는 레드(`#E85D5D`).
- **레이아웃**: 상단 히스토리 티커 → 중앙 코인 + 베팅 → 하단 CTA. 단일 컬럼 중심, 사이드바 없음(모바일 퍼스트).
- **베팅 UI**: SOL 인풋 + 프리셋 버튼(0.05, 0.1, 0.25, 0.5, 1) + 1/2x, 2x 배율 버튼. 깔끔한 인라인 구조.
- **핵심 원칙**: **기능 우선 UI**. 장식 최소, 인풋과 CTA가 시각적 주인공.

### 1-3. Coinflip Win Screen (`ref-coinflip-win.png`)
- **컬러**: 같은 네이비 베이스에 승리 금액 그린(`#4ADE80`). 코인은 실버/그레이.
- **타이포**: 모노스페이스/픽셀 폰트. "YOU WON" 대문자 + 금액은 그린 강조.
- **셀레브레이션**: 과도한 파티클/폭죽 없음. 코인이 결과면을 보여주고, 텍스트만으로 승리를 전달. **절제된 승리 연출**.
- **CTA**: "DOUBLE OR NOTHING" 단일 버튼이 명확.

### 1-4. Coinflip Light Mode (`ref-coinflip-light.png`)
- **컬러**: 따뜻한 오프화이트(`#F0EDEA`), 골드/옐로(`#F5B731`) 악센트. 블랙 텍스트 + 두꺼운 아웃라인 스타일.
- **타이포**: 같은 모노스페이스지만 **굵은 아웃라인 + 핸드드로잉 느낌**. 라이트모드에서는 보더가 시각적 구조를 담당.
- **핵심 원칙**: 다크/라이트 전환 시 **악센트 색상은 유지하되, 구조적 요소(보더/그림자)의 역할이 바뀜**.

---

## 2. 현재 문제점 진단

| 문제 | 심각도 | 원인 |
|------|--------|------|
| **네온 과다** — 모든 요소가 `#00FF41` 발광 | 🔴 | 하나의 그린이 배경/보더/텍스트/글로우/CTA에 전부 사용됨. 위계 없음. |
| **시각적 흐름 없음** — 눈이 어디로 가야 할지 모름 | 🔴 | 모든 패널이 동일한 보더 글로우. CTA가 묻힘. |
| **Provably Fair 중복** — 같은 정보가 badge + 패널 두 곳 | 🟡 | 모바일 v2에서 부분 제거했으나 완전하지 않음 |
| **리볼버가 너무 큼** — 화면의 40% 차지 | 🟡 | `min(240px, 60vw)`. 모바일에서 특히 압도적. |
| **globals.css 2177줄** — 미디어쿼리 3개(`1200/768/480px`)에 중복 스타일 다수 | 🔴 | Tailwind 없이 vanilla CSS로 반응형 관리 중 |
| **`>>> SELECT YOUR BET <<<`** — 촌스러움 | 🟡 | 터미널 감성이지만 레퍼런스들은 훨씬 절제됨 |
| **배경 파티클/그리드** — 산만함 | 🟡 | `body::before` 그리드 + `.vignette` + 여러 오버레이 |

---

## 3. 디자인 방향 — "Clean Dark Gambling"

레퍼런스 공통점에서 추출한 **미학적 원칙**:

### 3-1. 컬러 전략: 단색 악센트 + 중성 팔레트
- Death.Fun → 차트레즈 하나. Coinflip → 오렌지/틸 듀얼. **둘 다 3색 이내.**
- 우리: `#00FF41` 네온 그린을 유지하되, **사용 범위를 CTA + 승리 금액 + 핵심 수치로 제한**.
- 나머지 UI → 그레이스케일(`#0A-#1A-#2A`) + 화이트 텍스트.

### 3-2. 시각적 위계: 글로우는 1곳만
- 레퍼런스에서 **글로우/발광은 CTA 버튼 하나에만** 적용됨.
- 패널 보더, 카드, 배지 등은 **1px solid 라인** 또는 **아예 보더 없음**.
- 현재 `borderGlow` 애니메이션이 여러 요소에 적용됨 → **CTA 하나로 축소**.

### 3-3. 타이포그래피: 픽셀 폰트는 로고 + 수치만
- 현재: `Press Start 2P`가 거의 모든 텍스트에 사용됨.
- 개선: `Press Start 2P` → 로고, 배팅 금액, 결과 수치. 나머지 → `Space Grotesk` (이미 `--body-font`로 정의돼있으나 미사용).

### 3-4. 레이아웃: 호흡 있는 간격
- 레퍼런스들은 **요소 간 간격이 넓음** (24-32px). 우리는 8-12px로 빽빽함.
- **카드 내부 패딩**: 최소 20px. 카드 간 갭: 최소 16px.

### 3-5. 배경: 단색 또는 미세 그라데이션
- Death.Fun: 순수 블랙. Coinflip: 단색 네이비.
- **그리드 패턴, 파티클, vignette 전부 제거.** 단색 다크 배경.

---

## 4. 컬러 팔레트 (신규)

```
/* === Background === */
--bg-primary:    #080C08;    /* 메인 배경 — 거의 블랙, 미세 그린 틴트 유지 */
--bg-surface:    #0E130E;    /* 카드/패널 배경 */
--bg-elevated:   #151A15;    /* 호버/액티브 상태 */
--bg-overlay:    rgba(0, 0, 0, 0.7);  /* 모달 오버레이 */

/* === Accent (네온 그린 — 사용 범위 제한!) === */
--accent:        #00FF41;    /* CTA, 승리 금액, 핵심 수치 ONLY */
--accent-muted:  #00CC34;    /* 호버 상태 */
--accent-subtle: rgba(0, 255, 65, 0.08);  /* 배경 틴트 (카드 호버) */

/* === Neutral === */
--white:         #F0F0F0;    /* 주요 텍스트 */
--gray-100:      #B0B0B0;    /* 보조 텍스트 */
--gray-200:      #707070;    /* 비활성 텍스트 */
--gray-300:      #404040;    /* 보더, 구분선 */
--gray-400:      #252525;    /* 비활성 보더 */

/* === Semantic === */
--danger:        #FF3B3B;    /* 에러, 패배 */
--warning:       #FFB020;    /* 경고 */
--success:       #00FF41;    /* = accent (승리) */

/* === Border === */
--border-default:  #252525;              /* 기본 1px 보더 */
--border-active:   rgba(0, 255, 65, 0.3); /* 활성 상태 */
--border-cta:      #00FF41;              /* CTA 버튼만 */
```

### 이전 vs 신규

| 요소 | 이전 | 신규 |
|------|------|------|
| 패널 보더 | `rgba(0,255,65,0.35)` + 글로우 | `#252525` (그레이, 글로우 없음) |
| CTA 버튼 | 같은 네온 보더 (묻힘) | `#00FF41` 솔리드 보더 + 미세 글로우 (유일) |
| 텍스트 | 거의 다 네온 | 화이트 기본, 네온은 수치만 |
| 배경 | 그리드 패턴 + vignette | 단색 `#080C08` |
| 카드 배경 | 복잡한 그라데이션 | 단색 `#0E130E` |

---

## 5. 타이포그래피 스케일

```
/* === Font Families === */
--font-display:  'Press Start 2P', monospace;   /* 로고, 배팅금액, 결과, H1 */
--font-body:     'Space Grotesk', sans-serif;    /* 본문, 라벨, 설명 */
--font-mono:     'JetBrains Mono', monospace;    /* 지갑주소, 해시, 코드 */

/* === Scale (body=16px 기준) === */
--text-xs:   0.625rem;   /* 10px — 배지, 미세 라벨 */
--text-sm:   0.75rem;    /* 12px — 보조 정보 */
--text-base: 0.875rem;   /* 14px — 본문 기본 */
--text-md:   1rem;       /* 16px — 강조 본문 */
--text-lg:   1.25rem;    /* 20px — 섹션 타이틀 */
--text-xl:   1.5rem;     /* 24px — 배팅 금액 */
--text-2xl:  2rem;       /* 32px — 결과 금액 */
--text-3xl:  2.5rem;     /* 40px — 히어로 (데스크탑) */
```

### 폰트 사용 규칙
| 요소 | 폰트 | 사이즈 | 색상 |
|------|-------|--------|------|
| 로고 "DEGEN ROULETTE" | display | text-lg | accent |
| 배팅 금액 (인풋) | display | text-xl | white |
| 결과 금액 "0.5 SOL" | display | text-2xl | accent (승리) / danger (패배) |
| 프리셋 버튼 "0.1" | body (semibold) | text-sm | white |
| 설명/라벨 | body | text-base | gray-100 |
| 지갑 주소 | mono | text-xs | gray-200 |

---

## 6. 레이아웃 구조

### 6-1. Desktop (≥1024px) — 3 Column
```
┌─────────────────────────────────────────────────┐
│                    HEADER                        │
├──────────┬──────────────────────┬───────────────┤
│          │                      │               │
│  HALL OF │     GAME AREA        │   LIVE FEED   │
│  DEGENS  │                      │               │
│          │  ┌──────────────┐    │               │
│  (리더보드)│  │   REVOLVER    │    │  (실시간 베팅) │
│          │  │   (max 200px) │    │               │
│  240px   │  └──────────────┘    │   240px       │
│  fixed   │                      │   fixed       │
│          │  ┌──────────────┐    │               │
│          │  │   BET PANEL   │    │               │
│          │  └──────────────┘    │               │
│          │                      │               │
├──────────┴──────────────────────┴───────────────┤
│                    STATS BAR                     │
└─────────────────────────────────────────────────┘
```

- 사이드바: `width: 240px`, `position: sticky`, `top: 64px`
- 게임 에어리어: `flex: 1`, `max-width: 600px`, `margin: 0 auto`
- 갭: `24px`

### 6-2. Tablet (768px–1023px) — 2 Column
```
┌─────────────────────────────────┐
│             HEADER              │
├─────────────────┬───────────────┤
│                 │               │
│   GAME AREA     │  LIVE FEED    │
│                 │  (compact)    │
│                 │               │
├─────────────────┴───────────────┤
│        HALL OF DEGENS           │
│        (horizontal scroll)      │
└─────────────────────────────────┘
```

- 게임: `flex: 1`. 라이브 피드: `width: 200px`.
- 리더보드: 하단 수평 스크롤 카드.

### 6-3. Mobile (<768px) — Game Always Visible
```
┌─────────────────────┐
│       HEADER  [🏆]  │
├─────────────────────┤
│    GAME AREA        │
│  ┌───────────────┐  │
│  │  REVOLVER     │  │
│  │  (약간 축소)   │  │
│  └───────────────┘  │
│  ┌───────────────┐  │
│  │  BET PANEL    │  │
│  │  (compact)    │  │
│  └───────────────┘  │
├─────────────────────┤
│  RECENT PLAYS       │
│  (컴팩트 3-4줄)      │
└─────────────────────┘
  🏆 → 바텀시트 (Leaderboard)
```

- **Game = 항상 노출** — 탭 전환 없음. 스크롤 없이 바로 보이는 영역.
- 리볼버: 현재보다 약간 축소 (데스크탑은 그대로)
- **Recent Plays**: 게임 아래 컴팩트 3-4줄 (실시간 베팅 결과)
- **Leaderboard**: 헤더의 🏆 버튼 → 바텀시트/모달로 열기 (평소 안 보임)
- 배팅 패널: 인라인 구조 `[SOL] [input] [−] [+]`

---

## 7. 컴포넌트 디자인 가이드

### 7-1. 카드/패널
```css
.panel {
  background: var(--bg-surface);
  border: 1px solid var(--border-default);  /* 그레이! 네온 아님 */
  border-radius: 12px;
  padding: 20px;
}
.panel:hover {
  border-color: var(--border-active);  /* 호버 시에만 미세 네온 */
  background: var(--bg-elevated);
}
```
**글로우 없음.** 호버 시에만 `border-active`로 힌트.

### 7-2. CTA 버튼 (BET 버튼 — 유일한 글로우 요소)
```css
.btn-cta {
  background: transparent;
  border: 2px solid var(--accent);
  color: var(--accent);
  font-family: var(--font-display);
  font-size: var(--text-md);
  padding: 14px 32px;
  border-radius: 8px;
  box-shadow: 0 0 20px rgba(0, 255, 65, 0.15);
  transition: all 0.2s;
}
.btn-cta:hover {
  background: rgba(0, 255, 65, 0.08);
  box-shadow: 0 0 30px rgba(0, 255, 65, 0.25);
}
.btn-cta:disabled {
  border-color: var(--gray-300);
  color: var(--gray-200);
  box-shadow: none;
}
```

### 7-3. 프리셋 베팅 버튼
```css
.btn-preset {
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  color: var(--white);
  font-family: var(--font-body);
  font-weight: 600;
  font-size: var(--text-sm);
  padding: 8px 16px;
  border-radius: 6px;
}
.btn-preset:hover {
  border-color: var(--gray-200);
}
.btn-preset.active {
  border-color: var(--accent);
  color: var(--accent);
  background: var(--accent-subtle);
}
```

### 7-4. 인풋 필드
```css
.input-bet {
  background: var(--bg-primary);
  border: 1px solid var(--border-default);
  color: var(--white);
  font-family: var(--font-display);
  font-size: var(--text-xl);
  padding: 12px 16px;
  border-radius: 8px;
  text-align: center;
}
.input-bet:focus {
  border-color: var(--accent);
  outline: none;
}
```

### 7-5. 리볼버 (게임 비주얼)
- **기존 이미지 에셋 유지** (`cylinder-512.png` — Mason 제작 + 챔버 오버레이)
- 데스크탑: 현재 크기 유지
- 모바일: 현재보다 약간 축소 (체감상 화면의 ~30% 이하)
- 결과 애니메이션: 기존 회전 로직 유지
- AI 이미지 교체 불필요 — 기존이 브랜드 아이덴티티

### 7-6. 헤더
```
┌─[🎯 DEGEN ROULETTE]─────────────[Stats]──[Connect Wallet]─┐
```
- 높이: `56px` (현재 과다하게 높음)
- 배경: `var(--bg-primary)` + `border-bottom: 1px solid var(--border-default)`
- 로고: `font-display`, `text-lg`, `var(--accent)`
- Wallet 버튼: `btn-preset` 스타일 (accent 아님, 중립)

### 7-7. 결과 오버레이
```css
.result-overlay {
  background: var(--bg-overlay);
  /* 풀스크린 센터 */
}
.result-win .amount {
  color: var(--accent);
  font-family: var(--font-display);
  font-size: var(--text-2xl);
  /* 미세 글로우만 */
  text-shadow: 0 0 20px rgba(0, 255, 65, 0.3);
}
.result-lose .amount {
  color: var(--danger);
}
```
- "YOU WON" / "YOU LOST" 텍스트 + 금액. 심플.
- 파티클/폭죽 제거. 텍스트 + 글로우로 충분.
- "PLAY AGAIN" 단일 CTA.

---

## 8. Tailwind 마이그레이션 전략

### Phase 1: 설치 & 토큰 정의
```bash
pnpm add tailwindcss @tailwindcss/postcss postcss
```

`tailwind.config.ts`:
```ts
export default {
  theme: {
    extend: {
      colors: {
        bg: { primary: '#080C08', surface: '#0E130E', elevated: '#151A15' },
        accent: { DEFAULT: '#00FF41', muted: '#00CC34', subtle: 'rgba(0,255,65,0.08)' },
        neutral: { 100: '#B0B0B0', 200: '#707070', 300: '#404040', 400: '#252525' },
        danger: '#FF3B3B',
        warning: '#FFB020',
      },
      fontFamily: {
        display: ['"Press Start 2P"', 'monospace'],
        body: ['"Space Grotesk"', 'sans-serif'],
        mono: ['"JetBrains Mono"', 'monospace'],
      },
      fontSize: { /* 위 스케일 참조 */ },
      borderRadius: { sm: '6px', md: '8px', lg: '12px' },
      spacing: { /* 8px grid */ },
    },
  },
}
```

### Phase 2: globals.css → Tailwind 클래스
- 컴포넌트별 인라인 스타일 + `className` → Tailwind 유틸리티
- 미디어쿼리 → `sm:`, `md:`, `lg:` 프리픽스
- 2177줄 → 목표 200줄 이하 (커스텀 애니메이션/키프레임만)

### Phase 3: 컴포넌트 리팩토링
- 각 컴포넌트에서 `.game-card`, `.bet-panel` 등 CSS 클래스 → Tailwind 유틸리티
- 반응형은 Tailwind 프리픽스로 통일

---

## 9. 제거 목록

| 요소 | 이유 |
|------|------|
| `body::before` 그리드 패턴 | 산만. Death.Fun도 단색 배경. |
| `.vignette` | 불필요한 오버레이 |
| `@keyframes borderGlow` (패널용) | CTA 이외 글로우 전부 제거 |
| `>>> SELECT YOUR BET <<<` | "Enter bet amount" 또는 플레이스홀더 없이 빈 인풋 |
| Provably Fair 중복 표시 | 배지 하나로 통일. 클릭 → 모달 |
| 배경 파티클/스캔라인 | 레퍼런스 중 어디도 사용 안 함 |
| 과도한 `text-shadow` 네온 | 결과 금액 1곳만 |

---

## 10. 구현 우선순위

| # | 작업 | 예상 비용 | 방법 |
|---|------|----------|------|
| 1 | Tailwind v4 설치 + 토큰 정의 | ~$1 | Sonnet |
| 2 | globals.css → Tailwind 마이그레이션 (레이아웃 구조) | ~$3 | Opus |
| 3 | 컬러 팔레트 교체 (네온 축소) | ~$2 | Sonnet |
| 4 | 타이포그래피 분리 (display/body/mono) | ~$1 | Sonnet |
| 5 | 컴포넌트별 반응형 리팩토링 | ~$5 | Opus |
| 6 | 배경/글로우/파티클 제거 | ~$1 | Sonnet |
| 7 | 데스크탑 3-col 레이아웃 | ~$3 | Opus |
| 8 | 모바일: Recent Plays 컴팩트 + Leaderboard 바텀시트 | ~$2 | Opus |
| 9 | 결과 오버레이 리디자인 | ~$2 | Sonnet |
| 10| E2E 재검증 | ~$2 | Sonnet |

**총 예상: ~$22, 약 2시간**

---

## 11. v0.dev 프로토타이핑 프롬프트 (제안)

```
Create a dark-themed Solana gambling dApp called "DEGEN ROULETTE" with Russian roulette mechanic.

Layout:
- Desktop: 3 columns — left sidebar (leaderboard), center (game), right sidebar (live feed)
- Mobile: single column with tab navigation at bottom

Design tokens:
- Background: #080C08 (near-black with green tint)
- Accent: #00FF41 (neon green, used SPARINGLY — only on CTA button and win amounts)
- Text: #F0F0F0 (primary), #B0B0B0 (secondary), #707070 (muted)
- Borders: #252525 (default gray, NOT green)
- Cards: #0E130E with 1px #252525 border, 12px radius, no glow

Components:
- Header: 56px height, logo in pixel font (Press Start 2P), wallet connect button
- Game area: revolver visual (max 200px), bet input with preset buttons (0.05, 0.1, 0.25, 0.5, 1 SOL), single CTA "BET X SOL"
- Leaderboard: compact card list with rank, username, profit
- Live feed: real-time bet stream with outcome indicators

Key principles:
- Neon green glow ONLY on CTA button. Everything else: gray borders, no glow.
- Typography: pixel font for logo/amounts only, sans-serif for everything else
- No grid patterns, no particles, no vignette. Clean dark background.
- Generous spacing (24px gaps, 20px card padding)
```

---

_이 문서를 기반으로 v0 프로토타이핑 → Tailwind 마이그레이션 → 구현 진행._
_Mason 리뷰 후 착수._
